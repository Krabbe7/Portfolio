For at prøve spillet skal du downloade mappen og åbne den i f.eks. VS code med live server, 
da jeg ikke har sat den op til en localhost, og f.eks. Firefox samt Chrome brokker sig hvis 
man vil åbne spillet fra mappen med en file:// requests.
